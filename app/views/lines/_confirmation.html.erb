<% if @lines.empty? %>
  Rajoutez un item en cliquant son prix.
<% else -%>
  <%= link_to "Confirmer", orders_path(:order => { :shop_id => @lines.first.shop_id }), :method => :post, :id => "confirm_link" %>
<% end -%>
