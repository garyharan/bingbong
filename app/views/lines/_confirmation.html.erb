<div id="confirmation">
  <% if @lines.empty? %>
    <p>Rajoutez un item en cliquant son prix.</p>
  <% else -%>
    <%= link_to "Confirmer", new_order_path(:order => { :shop_id => @lines.first.shop_id }), :id => "confirm_link" %>
  <% end -%>
</div>
